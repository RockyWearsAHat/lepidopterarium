{{!-- Thankfully we can access any Lepo attributes simply with their name --}}

<div class="butterfly-contatiner">
  <a href="/butterfly/{{id}}"
    ><button class="butterfly-btn" id="{{id}}">{{name}}</button></a
  >
  <img class="tile-img3" src="/images/{{images.[0]}}" alt="{{name}}" />
</div>

{{!--<button id="{{id}}">
  <a class="butterfly-container" href="/butterfly/{{id}}">
    <p>{{name}}</p>
    <img class="tile-img3" src="../{{images.[0]}}" alt="{{name}}" />
  </a></button
>--}}
